 CREATE TABLE characters (
    id SERIAL PRIMARY KEY,
    username VARCHAR(255) NOT NULL,
    reputation INTEGER NOT NULL DEFAULT 1 CHECK(reputation >= 1 AND reputation <= 9999),
    createDate DATE NOT NULL
);